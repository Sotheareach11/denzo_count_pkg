# Runtime dependencies only.
# Dev/lint/test tools live in requirements-dev.txt.
# This file is kept so CodeSandbox / devcontainer `pip install -r requirements.txt`
# keeps working; pyproject.toml is the canonical source.

Flask==3.0.3
pdfplumber==0.11.4
openpyxl==3.1.5
pypdf==5.9.0
gunicorn==21.2.0
